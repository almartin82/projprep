# #everything here gets run before tests
# pecota_file <- file.path('paid_projections', 'pecota_2016.xls')
# pecota_file_loc <- file.path(getwd(), pecota_file)
# #for CMD check
# pecota_file_loc <- gsub('.Rcheck', '', pecota_file_loc, fixed = TRUE)
# #for devtools::test()
# pecota_file_loc <- gsub('tests/testthat/', '', pecota_file_loc, fixed = TRUE)
# pecota_ex <- read_raw_pod(pecota_file_loc)
